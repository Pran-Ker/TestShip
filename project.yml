name: TestShip
options:
  bundleIdPrefix: com.hebbarpran
  deploymentTarget:
    iOS: "17.0"
  xcodeVersion: "16"

settings:
  base:
    MARKETING_VERSION: "1.0"
    CURRENT_PROJECT_VERSION: "1"
    SWIFT_VERSION: "5.10"
    IPHONEOS_DEPLOYMENT_TARGET: "17.0"

targets:
  TestShip:
    type: application
    platform: iOS
    deploymentTarget: "17.0"
    sources:
      - Sources/TestShip
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.hebbarpran.testship
        INFOPLIST_FILE: Sources/TestShip/Info.plist
        CODE_SIGN_STYLE: Manual
        DEVELOPMENT_TEAM: 7M3BLC8QM2
        PROVISIONING_PROFILE_SPECIFIER: match AppStore com.hebbarpran.testship
